# はじめに

ThreadsとXでの発信、反応の集計、商品と売上の管理を、ひとつの日本語
ダッシュボードから行うためのシステム。

## いまどこまで動くか

- 認証と6画面（概要・投稿・カレンダー・分析/改善・商品/売上・設定）
- 投稿の作成・承認・予約・予約取消、売上の手動入力とCSV取込
- 送信ワーカー（模擬接続で通し確認できる。実APIの認可はまだ無い）
- 費用の予約と精算（**単価が未登録のうちは有料処理が動かない**）

**まだ実際の投稿・実際のメール送信・実際の課金は一度も行っていない。**

## 読む順番

1. `README.md` … 動かし方（構築・起動・テスト・停止と復旧・費用の確認先）
2. `HANDOFF.md` … 完成した操作 / 検証方法 / 未接続・未検証 / 次の段階
3. `AGENTS.md` … コードを触るときの約束

## いちばん最初にやること

```bash
npm install
cp .env.example .env.local     # DATABASE_URL と TOKEN_ENCRYPTION_KEY を設定
npm run migrate
npm run admin:create -- you@example.com "あなたの名前"
npm run seed
npm run dev
```

`http://localhost:3000` を開いてログインする。

初期状態では**すべての送信が停止**しており、媒体は未接続、AI生成も無効。
その状態でも下書きの作成と売上の手動入力はできる。

## 取り込み済みのもの

キット（Google Driveの共有フォルダ）の内容を取り込み済み。

- 既存のThreads投稿90件（すべて DRAFT。予約日時は入れていない）
- 本人の経験3件（`content/operator-facts.json`）
- 配信メール5通、販売原稿、チェックシート
- 試作GAS（`reference/`。参考用で、新システムと並行稼働させない）

**人の実体験として使えるのは3件だけ。** 顧客情報・未承認の実績・
架空の効果数値は生成も公開もしない。

## 次にやること

`HANDOFF.md` の「必要なユーザー操作」を上から順に。
最初は料金の確認（設定するまで有料処理は動かない）。
