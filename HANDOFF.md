# 引継ぎメモ

最終更新: Phase 3 まで実装。Phase 4（メール・無料資料・実投稿）は未着手。

## キットの所在について（訂正）

初回の作業時、キットはリポジトリにもローカルにも無く、「存在しない」と
報告した。**これは探し方が足りなかった。** キットはGoogle Driveの
共有フォルダにあった。

- フォルダ: https://drive.google.com/drive/folders/1Xa7pKkxSvtNCVWqqc9NknMgnEDp44mxi
- `AI発信自動化_ClaudeCode開発キット.zip`

現在はキット一式を取り込み済み。

| キットのファイル | 取り込み先 |
| --- | --- |
| `src/domain/policy.mjs` | 同じ場所（**未改変**） |
| `tests/policy.test.mjs` | `tests/kit/policy.test.mjs`（import先のみ調整） |
| `content/operator-facts.json` | 同じ場所。本人の経験3件 |
| `content/threads-90-posts.json` | 同じ場所。90件 |
| `content/email-sequence.json` | 同じ場所。5通 |
| `content/note-sales-copy.txt`, `cheat-sheet.txt` | 同じ場所 |
| `content/prompt-library.zip`, `starter-pack.zip` | 同じ場所 |
| `reference/Code.gs` ほか | 同じ場所。試作GAS（参考用） |
| `CLAUDE_CODE_PROMPT.md` | `reference/CLAUDE_CODE_PROMPT.original.md` |

`npm test` はキットの12件を含めて実行する。`policy.mjs` は改変していない。

### 試作との突き合わせで見つかった実装の誤り

`src/domain/policy.mjs` とアプリ実装を突き合わせ（`tests/kit/cross-check.test.ts`）、
**アプリ側の誤りを2件見つけて直した。** 詳細は `tests/kit/DIFFERENCES.md`。

1. **上限を暦日で数えていた。** JSTの暦日で「1日3件」としていたため、
   23:00に3件・翌00:30に3件が通り、実質2時間半で6件になりえた。
   試作と同じ**直近24時間の移動窓**へ変更した。
2. **送信直前に上限を見ていなかった。** 予約時にしか数えておらず、
   予約の遅延や後から公開された投稿で前提が崩れていた。
   送信直前に**実際に公開された時刻**で数え直すようにした。

## 完成した操作

### Phase 1（認証・DB・6画面）

| できること | 場所 |
| --- | --- |
| ログイン / ログアウト | `/login` |
| 収支の確認（売上・手数料・実費・税引前収支・目標との差） | `/dashboard` |
| 投稿の作成・編集・承認・予約・予約取消 | `/posts` |
| 予約と公開のカレンダー表示（JST） | `/calendar` |
| 反応の比較と改善提案の採否 | `/analysis` |
| 商品一覧、売上の手動入力とCSV取込 | `/products` |
| 接続状態・停止・AI・予算・知識の承認 | `/settings` |

- 全画面・全APIが認証で保護され、他人のデータは見えない・操作できない。
- 販売手数料が未入力なら、税引前収支と目標との差を**確定値として表示しない**。
- 売上0件・投稿0件・反応0件は空状態で表示し、架空のグラフで埋めない。
- 承認後に本文を変えると承認が外れ、予約も解除される（再承認が必要）。

### Phase 2（送信）

- Threads（コンテナ作成→状態確認→公開）とXのアダプター。
- ジョブの原子的な取得（`FOR UPDATE SKIP LOCKED`）、期限付き claim。
- 送信直前の停止確認、送信前の attempt 永続化。
- 結果不明は `UNKNOWN` へ隔離し、**再送しない**。照合ジョブを積む。
- 期限切れ（24時間超過）は `EXPIRED`。まとめて投稿し直さない。
- 模擬接続（`simulated`）と本番接続（`live`）を明確に分けてある。

### Phase 3（AI・費用・指標）

- 料金表と為替が未登録なら、有料の自動処理は**APIを呼ぶ前に止まる**。
- 見込額の予約 → 実行 → 実績で精算。課金有無が不明なら予約を解放しない。
- 生成物は厳密に検証してから DRAFT として保存。自動承認しない。
- 一般知識しか参照していない文面を本人の経験として書かせない。
- 指標は 1h / 24h / 72h / 168h のバケットで取得。欠測は NULL のまま。

## 検証方法

```bash
npm run build   # HTTP経路のテストに必要
npm test        # 217件
npm run typecheck
```

DBテストには `TEST_DATABASE_URL`（開発用とは別のDB）が必要。未設定なら
該当テストはスキップされる。

重点的に確かめてあること:

| 確認したいこと | テスト |
| --- | --- |
| 状態遷移（段飛ばし・UNKNOWNからの再送不可） | `tests/domain/post-state.test.ts` |
| 同時claim（16並列で1件、30件×20並列で重複なし） | `tests/db/jobs.test.ts` |
| 送信後のタイムアウト（PUBLISHING中の期限切れ→UNKNOWN隔離） | `tests/db/jobs.test.ts` |
| 予算の同時予約（枠3,000円に400円×20並列→7件のみ） | `tests/db/budget.test.ts` |
| 有料呼出しの同時実行で枠・月次予算を超えない | `tests/db/ai-budget.test.ts` |
| 無権限操作（未ログインで6画面と12のAPI経路） | `tests/db/api-auth.test.ts` |
| 売上の重複取込（注文IDでファイル内/DB既存を分けて排除） | `tests/domain/sales-import.test.ts` |
| メール解除（GETで有効化しない、停止中でも解除可） | `tests/domain/email-consent.test.ts` |
| 送信直前の停止・未接続・期限切れで外部へ送らない | `tests/db/worker.test.ts` |

画面はPC（1360px）とスマホ（390px）で全6画面を表示確認済み。
どちらも本文の横スクロールは発生しない（表とカレンダーのみ個別に横スクロール）。

## 未接続・未検証

**実際のAPIに一度も接続していない。** 以下は仕様に沿って実装したが、
本番の応答で検証していない。

| 項目 | 状態 |
| --- | --- |
| Threads API | エンドポイントとフィールド名を**要確認**。認可なし |
| X API | 同上。認可なし |
| 指標のフィールド名 | どの指標が実際に取れるかは権限次第。**要確認** |
| 指標取得のAPI費用 | **未確認**。取得頻度を決める前に確認が必要 |
| Anthropic API | 呼出し経路はテスト済み（fetchを差し替え）。実接続なし |
| AIの単価・為替 | **未登録**。登録するまで有料処理は動かない |
| ホスティング / cron / DB / メールの費用 | **未確定**。下記参照 |

### 費用が未確定であること

「月1万円以内」に収まる構成を選ぶ前提だが、**各サービスの現在の料金を
確認していないため、どの組み合わせが1万円以内かを確定できない。**
既存の有料プランがあるとも仮定していない。

決める前に確認が必要なもの: ホスティング（Next.jsのサーバー実行とcron）、
PostgreSQL（Neonなどの候補）、メール配信サービス、独自ドメイン。

コードは特定のホスティングに依存していない（`DATABASE_URL` と
`worker:once` を呼べる cron があれば動く）ので、料金を確認してから選べる。

### デプロイ先での未対応

Vercel などのサーバーレス環境へ置いた場合、**ワーカーが動かない。**
常駐プロセスを置けず、cron から叩けるHTTPの入口も未実装。

影響するのは投稿の送信・指標の取得・期限切れの処理・照合。
下書きの作成/編集/承認/予約、売上の入力、各画面の表示は動く。
（送信は初期状態で停止しているので、動かなくても実害は出ていない）

対応するには `POST /api/cron/tick` を作り、`CRON_SECRET` で認可して
`tick()` を呼ぶ。`vercel.json` の crons から5分おきに叩く。

### 未実装（Phase 4）

- メールの登録・確認・解除の画面とAPI（**ドメインのロジックとテストは
  実装済み**だが、画面・API・実際の送信は未実装）
- 無料資料の配布導線
- noteの販売リンクの登録UI（DBの列とseedの器はある）
- クリックIDの発行と着地の記録（テーブルはある）
- 改善提案の**自動生成**（保存・検証・採否のUIはあるが、指標から提案を
  作る処理は未実装）
- 媒体のOAuth接続フロー（現在はコマンドでトークンを登録する）

## 必要なユーザー操作（まとめて）

チャットに認証情報を貼らないこと。すべて手元の環境変数かコマンドで設定する。

1. **PostgreSQLを用意し** `DATABASE_URL` を設定する
2. **`TOKEN_ENCRYPTION_KEY` を生成**して設定する
   （`node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`）
3. **所有者アカウントを作る**（`npm run admin:create -- <email>`）
4. **AIの単価と為替を確認して設定する**（設定するまで有料処理は動かない）
5. **ホスティング・DB・メールの料金を確認して構成を決める**
6. **ThreadsとXの開発者アカウントで認可を取得する**（申請が必要）
7. 認可が取れたら `npm run account:connect -- <platform> live <user_id>`
8. **設定画面で停止フラグを解除する**（解除するまで送信されない）
9. **noteの商品URLを登録する**（公開後。CTAの許可先になる）
10. **X向けの文面を用意する**（90投稿はThreads向け。Xは別の下書き）

デプロイ先の初期設定（Vercel 等）:

```bash
export DATABASE_URL='<本番の接続文字列>'
export DATABASE_SSL=require
export TOKEN_ENCRYPTION_KEY='<ホスティングと同じ値>'
npm run migrate
npm run admin:create -- you@example.com "あなたの名前"
npm run seed && npm run import:posts
```

状態は `GET /api/health` で確認できる。

知識と既存投稿はキットから取り込み済みなので、記入作業は不要。
知識を増やす場合は `content/operator-facts.json` に本人が提供できる
ものだけ追記する。**本人の経験として使えるのは現在の3件だけ。**

## 次の段階

1. **料金を確認して構成を確定する**（4, 5）。ここが決まるまで有料処理は
   止まったままで安全。
3. **模擬接続で通しの動作を確認する**
   （`npm run account:connect -- threads simulated` → 承認 → 予約 →
   `npm run worker:once`）。外部へは送信されない。
4. **媒体の認可を取得し**（8）、本番接続に切り替えて1件だけ実投稿する。
5. Phase 4（メール・無料資料）に着手する。ドメインのロジックは既にあるので、
   画面・API・配信サービスの接続から。

## やってはいけないこと（実装上の約束）

- `UNKNOWN` の投稿を自動で再送しない。照合してから確定する。
- 課金有無が不明な失敗で予算の予約を解放しない。
- 取得できない指標を0として記録しない。
- 少ない標本で勝ちパターンを断定しない。
- 一般知識を本人の経験として書かない。
- 事実の新規追加・商品の価格変更・新商品の公開を自動化しない。
- 収益の保証、達成時期の断定をしない。
- 期限切れの予約をまとめて投稿し直さない。
- 新旧のスケジューラーを同時に起動しない。
- noteの非公開APIやログインCookieを流用しない。
- 指定されたテスト宛先以外へメールを送らない。
