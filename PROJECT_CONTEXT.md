# 前提と制約

実装の判断がなぜそうなっているかの背景。

## 目的

ThreadsとXでAI/Web制作に関する投稿を行い、無料教材とnoteの販売商品へ
誘導する。投稿の反応を集計し、根拠付きの改善案を管理者が判断して次の
投稿に反映する。

## 固定条件

- 新規アカウントから開始。実績も購読者も0件から。
- 月利益目標 300,000円、運営費上限 10,000円。
- **収益保証や達成時期の断定は禁止。**
- 広告は初期0円。
- **企業実績や顧客素材は公開できない。**

これらは設定値ではなく前提。「実績0件」を隠すためにダミーの数字を置く、
といった実装をしない。

## 商品

| コード | 内容 | 価格 |
| --- | --- | --- |
| `starter` | Starter（3本） | 1,980円 |
| `bundle10` | 全10本 | 9,800円 |

**全10本にはStarterの3本が含まれる。** すでにStarterを買った人へ
二重購入を勧めない。これは現行の販売案であって確定ではない。

商品の軸:

1. **意図整理** … 依頼をAIで整理し、本人が確認して制作AIへ渡す
2. **デザイン具体化** … 参考サイトを、余白・文字・レイアウト・導線・
   動きの指定へ変換する
3. **品質判断** … 操作する側が基礎知識を持ち、デザイン・導線・動作の
   正否を判断する

## 知識の扱い

`content/operator-facts.json` は**本人が提供した知識**として扱う。

- 本人が使ったAI名の組合せ、時間削減、受注数、売上改善、顧客名は
  **補完しない**。
- **一般的なノウハウと本人の経験は別種の情報**として保存する
  （`knowledge.kind` が `general` / `operator_experience`）。
- 一般知識しか参照していない文面を、本人の経験として書かせない
  （`src/domain/knowledge.ts` で検出する）。

これは体裁の問題ではない。事実でないことを本人の実績として発信すると、
発信そのものが成立しなくなる。

## 販売チャネル

事業の軸は2つある。**管理は一つの画面で行い、数字と導線は分ける。**

| チャネル | 商品の性質 | 導線 |
| --- | --- | --- |
| note | 教材の売り切り（digital_product） | Threads/X → 無料資料 → note |
| ココナラ | 役務提供（service。納期と作業が発生） | 別系統の入口 |

**ココナラは外部への誘導を禁止している。** 出品ページから note や無料資料へ
誘導する記載は規約違反で、アカウント停止の対象になりうる。したがって
導線は一方通行になる。

- Threads/X → ココナラ … 可
- ココナラ → note / 無料資料 … **不可**

このため、投稿のCTAに使えるURLは `channel = 'note'` の商品に限っている
（`src/ai/generate.ts`）。ココナラへ誘導したくなった場合は、規約を確認
したうえでここを明示的に変えること。

納期・返金条件・原価の考え方が違うものを同じ数字として混ぜると利益の
内訳が読めなくなるので、`sales.channel` と `products.kind` で分けて持つ。

## 販売とデータ取得

- 販売ページは既存のものを再利用し、ダッシュボードと混同しない。
- **noteやココナラの非公開APIやログインCookieを流用しない。** 公式に
  対応可能な購入データ取得が確認できないため、売上は手動入力かCSV取込。
- ココナラは「売上管理・振込申請」から**全期間の売上CSV**を落とせる
  （PC版のみ）。毎回全件が出力されるが、注文IDの一意制約で既存分は
  弾かれるため、同じファイルを何度取り込んでも増えない。
- UTMと内部クリックIDは記録できるが、**個別の購入と投稿を紐付けられる
  とは仮定しない**。クリック数は観測値として表示し、購入寄与としては
  扱わない（`src/domain/metrics.ts` の `attributeSales`）。

## 費用の考え方

月次10,000円の内訳（AI 3,000 / X 3,000 / インフラ 2,000 / 予備 2,000）は
**自社の予算配分であって、各社の料金ではない。**

- 有料呼出しの前に最大見込額を予約し、枠を超えるなら拒否する。
- **料金や為替が未登録なら有料の自動処理を止める。** 推測した単価で
  課金判断をしない。
- 費用の反映には提供元側の遅延があるため、**「絶対に超えない」とは
  表示しない。**
- API未接続でも下書き編集と手動の売上入力は使える。

## メール

明示同意 → 確認後に登録有効化 → 解除・配信履歴・重複防止が条件。

- **確認用のGETだけで登録を有効化しない**（確認はPOST）。
- **解除は配信停止中でも常に有効。**
- 未確認・解除済みへは送らない。
- **外部の人へのテスト送信は禁止。** 指定されたテスト宛先に限る。

## 由来について

指示書には別会話で作成したGAS（`reference/Code.gs`）への言及があるが、
このリポジトリには存在しなかった。重複送信防止・配信停止の意図は
指示書の記述から `src/domain/` に実装している（`policy.ts`,
`email-consent.ts`）。稼働中のものとは扱っていない。

企業への自動営業メールのプロジェクトとは別物であり、その機能は入れない。
